/**
 * @param {string} s
 * @param {number} numRows
 * @return {string}
 */
var convert = function(s, numRows) {
    
};

var a = [[1, 2], [3, 4]];
var b = [1, 2];
a.forEach(v => {
    console.log(v);
})