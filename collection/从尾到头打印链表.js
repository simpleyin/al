/**
 * 题目：给出链表头，从尾部反向依次打印值。
 * 
 *
 */
function Node(val) {
    this.val = val;
    this.next = null;
}

function printListReversingly(node) {
    //1. 用一个栈来记录每次遍历的值，最后出栈打印
    //2. 使用递归先打印每各节点后续节点的值
}

printListReversingly();